docker container ls -a                                       =list of containers
docker images                                                =list of images
docker build -t firstimage .                                 =create a custom image
docker run -d -p 82:80 --name apacheinstance firstimage      =create a container that runs your image
docker run -d -p 5051:5000 --name flaskinstance flaskimage   = create a container with an image
docker tag firstwebsite:latest georgiana04/firstwebsite:latest  = change the name of the image
docker rmi firstwebsite:latest                                  = delete an image
docker push georgiana04/flaskimage:latest                       = push an image to the Docker Hub repo
docker rm flaskinstance -f                                   =delete a container
docker run -d -p 82:80 --name apacheinstance georiana04/firstimage      =create a container that runs your image from Docker Hub



docker exec -it containername /bin/bash = to get inside the container and modify the image
apt update
apt install nano
nano index.html = to get inside the html file to modify the image
exit = exit the container
docker commit containername newimagename = create a new image with the previous changes
docker history userrname/imagename = how the image was created and to see the changes made to the image
docker inspect = shows the exact state of how is the image now
dive imagename = current layer contents


docker network create networkname = to create a network
docker run -d -p 6379 --network network1 --name redis redis:alpine = creates the Redis application 

docker run -p 83:80 --name flask83 -d -e TEAM="Team1" flask-team-img    -create a container and specify a value for a variable
docker run -d -p 99:80 --name apache99 --memory 50mb httpd   = create a container with a specified memory capacity
docker run -d -p 94:80 --name apache94 --cpu-quota=25000 --cpu-period=100000 httpd   = to saz how much cpu it can use (25%)

aws ecr create-repository --repository-name awsgeoimage =create a repo on ECR on the AWS console